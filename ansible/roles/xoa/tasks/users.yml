# roles/xoa/tasks/users.yml
---
- name: Create operator user with xo-cli
  shell: |
    xo-cli register --allowUnauthorized http://127.0.0.1 admin@admin.net admin
    xo-cli user.create email=cgscheible@yahoo.com password=test
  become: true

